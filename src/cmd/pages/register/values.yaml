name: pages-register
labels: [ auth-frontend ]

# baseUrl:
# image:

path: "/register"
secure: false

env:
  SUBMIT_URL: "{{ .baseUrl }}/register"
  LOGIN_URL: "{{ .baseUrl }}/login"
  REDIRECT_URL: "{{ .baseUrl }}/login?registered=true"
  REGISTER_API_URL: "http://apis-register.{{ .Release.Namespace }}.svc.cluster.local"